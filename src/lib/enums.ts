export enum ProcedureCategory {
  REAL_ESTATE = 1,
  VEHICLE = 2,
  CIVIL = 3,
}

export enum AuthorityLevel {
  COMMUNE = 1,
  DISTRICT = 2,
  PROVINCE = 3,
  CENTRAL = 4,
}

export enum LocationLevel {
  PROVINCE = 1,
  DISTRICT = 2,
  WARD = 3,
}

export enum PreparedBy {
  BUYER = 1,
  SELLER = 2,
  BOTH = 3,
}
